<?php namespace SCT\Settings;

/**
 * Interface SettingsInterface
 *
 * Interface for all setting classes to follow
 *
 * @package Settings
 */
interface SettingsInterface
{
    public static function init($environment);
}